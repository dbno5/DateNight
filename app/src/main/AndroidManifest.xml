<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.billyji.datenight"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/plate"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/plate"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >

        <activity
            android:name="com.billyji.datenight.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="sensorPortrait"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/yelp_api_key" />

        <activity
            android:name="com.billyji.datenight.activities.FoodChoiceActivity"
            android:label="@string/title_food_choices"
            android:screenOrientation="sensorPortrait"
            android:parentActivityName="com.billyji.datenight.activities.MainActivity"
            >

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.billyji.datenight.activities.MainActivity" />

        </activity>

    </application>

</manifest>